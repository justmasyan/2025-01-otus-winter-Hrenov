spring:
  data:
    mongodb:
      database: test_db
      port: 0

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  change-logs-scan-package: ru.otus.hw.mongock.test_changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local